Перем ПредупреждениеОНедоступностиСпискаПользователейПоказано;

#Область СведенияОВнешнемОтчете

// Обязательная функция - содержит параметры работы с обработкой  
// 
Функция СведенияОВнешнейОбработке() Экспорт 
	
	РегистрационныеДанные = Новый Структура;
	
	//       * Наименование - Строка - Необязательный. Представление для администратора (наименование элемента справочника). 
	//           Если не заполнено, то берется представление объекта метаданных внешнего объекта. 
	РегистрационныеДанные.Вставить("Наименование", "АнализЗамеровПроизводительности");
	
	//       * БезопасныйРежим - Булево - Необязательный. Признак подключения внешней обработки в безопасном режиме. 
	//           Значение по умолчанию Истина (обработка будет выполняться безопасно). 
	//           Подробнее - см. разделы справки "ВнешниеОтчетыМенеджер.Подключить" и "ВнешниеОбработкиМенеджер.Подключить". 
	РегистрационныеДанные.Вставить("БезопасныйРежим", Ложь);
	
	//       * Версия - Строка - Версия объекта. 
	//           Задается в формате: "<Старший номер>.<Младший номер>". 
	РегистрационныеДанные.Вставить("Версия", "1.0");
	
	//       * ВерсияБСП - Строка - Необязательный. Версия библиотеки, на которую рассчитывают механизмы внешнего объекта. 
	//           Подробнее - см. СтандартныеПодсистемыСервер.ВерсияБиблиотеки(). 
	РегистрационныеДанные.Вставить("ВерсияБСП", "");
	
	//       * Вид - Строка - Вид внешнего объекта. Соответствует реквизиту "Вид" (ПеречислениеСсылка.ВидыДополнительныхОтчетовИОбработок). 
	//           Для определения вида можно использовать функции "Вид*" общего модуля ДополнительныеОтчетыИОбработкиКлиентСервер, 
	//           или указать вид явно: 
	//           ** "ПечатнаяФорма" 
	//           ** "ЗаполнениеОбъекта" 
	//           ** "СозданиеСвязанныхОбъектов" 
	//           ** "Отчет" 
	//           ** "ДополнительнаяОбработка" 
	//           ** "ДополнительныйОтчет" 
	РегистрационныеДанные.Вставить("Вид", "ДополнительныйОтчет");
	
	//       * Информация - Строка - Необязательный. Краткая информация по внешнему объекту. 
	//           В этом параметре для администратора рекомендуется указать описание возможностей внешнего объекта. 
	//           Если не заполнено, то берется комментарий объекта метаданных внешнего объекта.  
	РегистрационныеДанные.Вставить("Информация", 
		"Отчет для анализа замеров производительности, выполненных с помощью подсистемы ""Оценка производительности"" 
		|из Библиотеки стандартных подсистем.");
	
	// Назначение 
	МассивНазначенийОбработки = ПолучитьНазначениеОбработки();
	РегистрационныеДанные.Вставить("Назначение", МассивНазначенийОбработки);
	
	// Команды 
	ТаблицаКомандОбработки = ПолучитьТаблицуКомандОбработки();
	РегистрационныеДанные.Вставить("Команды", ТаблицаКомандОбработки);
		
	// Результат функции 
	Возврат РегистрационныеДанные;
	
КонецФункции

// Служебная функция обработки Экспорт - не обязательно  
// 
Функция ПолучитьНазначениеОбработки() Экспорт 
	
	// Инициализация массива 
	МассивНазначенийОбработки = Новый Массив;
	
	// Заполняем массив 
	//МассивНазначенийОбработки.Добавить("Справочник.Организации"); 
	
	/////////////////////////////////////////////////////////////////////////////// 
	//// При добавлении объекта в массив назначений следует убедиться, что в 
	//// его формах (в основном объекта и списка) прописан код по использованию 
	//// подсистемы дополнительных обработок 
	////  
	//// Например, в форме объекта в процедуре "ПриСозданииНаСервере" прописать: 
	//// ДополнительныеОтчетыИОбработки.ПриСозданииНаСервере(ЭтаФорма); 
	//// *текст кода может меняться в зависимости от версии БСП 
	
	// Результат функции 
	Возврат МассивНазначенийОбработки;
	
КонецФункции

// Служебная функция обработки Экспорт - не обязательно  
// 
Функция ПолучитьТаблицуКомандОбработки() Экспорт 
	
	// Инициализация таблицы 
	// Состав может меняться в зависимости от версии БСП 
	тзКоманд = Новый ТаблицаЗначений;
	тзКоманд.Колонки.Добавить("Идентификатор"           , Новый ОписаниеТипов("Строка")); // Это может быть любая текстовая строка, уникальная в пределах данной обработки 
	тзКоманд.Колонки.Добавить("Представление"           , Новый ОписаниеТипов("Строка")); // Представление команды в пользовательском интерфейсе 
	тзКоманд.Колонки.Добавить("ПоказыватьОповещение"    , Новый ОписаниеТипов("Булево")); // Надо показывать оповещение или нет 
	тзКоманд.Колонки.Добавить("Модификатор"             , Новый ОписаниеТипов("Строка")); // Дополнительный модификатор команды 
	тзКоманд.Колонки.Добавить("Использование"           , Новый ОписаниеТипов("Строка")); // Вирант использования команды (см. ниже) 
	тзКоманд.Колонки.Добавить("Скрыть"                  , Новый ОписаниеТипов("Булево")); // Серверная команда, которая вызывается из формы внешней обработки. 
	
	// Колонки ВариантЗапуска и Использование можно и желательно использовать по одиночке 
	// В случае указания обеих их значения должны совпадать 
	
	// Варианты для значения колонки "Использование": 
	//  ОткрытиеФормы – просто открывает форму обработки. 
	//  ВызовКлиентскогоМетода – будет вызвана клиентская процедура из формы внешней обработки. 
	//  ВызовСерверногоМетода  - будет вызвана серверная процедура из модуля обработки. 
	//  СценарийВБезопасномРежиме – серверная процедура в безопасном режиме. 
	
	ДобавитьКоманду(тзКоманд, "АнализЗамеровПроизводительности", "Анализ замеров производительности", Ложь, "ОткрытиеФормы", "", Ложь);
	
	// Результат функции 
	Возврат тзКоманд;
	
КонецФункции

// Добавление команды в таблицу команд 
// 
Процедура ДобавитьКоманду(ТаблицаКоманд, Идентификатор, Представление, ПоказыватьОповещение, Использование, Модификатор, Скрыть)
	
	строкаКоманды = ТаблицаКоманд.Добавить();
	строкаКоманды.Идентификатор = Идентификатор;
	строкаКоманды.Представление = Представление;
	строкаКоманды.ПоказыватьОповещение = ПоказыватьОповещение;
	строкаКоманды.Использование = Использование;
	строкаКоманды.Модификатор = Модификатор;
	строкаКоманды.Скрыть = Скрыть;
	
КонецПроцедуры

// Обязательная процедура внешней обработки  
// 
Процедура ВыполнитьКоманду(ИдентификаторКоманды, ПараметрыВыполненияКоманды = Неопределено) Экспорт
	
	
КонецПроцедуры

#КонецОбласти

#Область НастройкиФормыОтчета

// Настройки общей формы отчета подсистемы "Варианты отчетов".
//
// Параметры:
//  Форма - ФормаКлиентскогоПриложения - форма отчета или форма настроек отчета.
//  КлючВарианта - Строка - ключ варианта отчета.
//  Настройки - Структура - настройки общей формы отчета.
Процедура ОпределитьНастройкиФормы(Форма, КлючВарианта, Настройки) Экспорт
	
	Настройки.События.ПередЗагрузкойНастроекВКомпоновщик = Истина;
	Настройки.События.ПриОпределенииПараметровВыбора = Истина;
	
КонецПроцедуры

// Добавляет стандартный пользовательский отбор по полю Пользователь во все варианты отчета.
// Отбор размещается в панели быстрых настроек и поддерживает множественный выбор.
Процедура ПередЗагрузкойНастроекВКомпоновщик(Контекст, КлючСхемы, КлючВарианта,
	НовыеНастройкиКД, НовыеПользовательскиеНастройкиКД) Экспорт
	
	ПолеПользователь = Новый ПолеКомпоновкиДанных("Пользователь");
	ЭлементОтбораПользователь = Неопределено;
	
	Для Каждого ЭлементОтбора Из НовыеНастройкиКД.Отбор.Элементы Цикл
		Если ТипЗнч(ЭлементОтбора) = Тип("ЭлементОтбораКомпоновкиДанных")
			И ЭлементОтбора.ЛевоеЗначение = ПолеПользователь Тогда
			ЭлементОтбораПользователь = ЭлементОтбора;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если ЭлементОтбораПользователь = Неопределено Тогда
		ЭлементОтбораПользователь = ОбщегоНазначенияКлиентСервер.ДобавитьЭлементКомпоновки(
			НовыеНастройкиКД.Отбор,
			"Пользователь",
			ВидСравненияКомпоновкиДанных.ВСписке,
			Новый СписокЗначений,
			НСтр("ru = 'Пользователи'"),
			Ложь,
			РежимОтображенияЭлементаНастройкиКомпоновкиДанных.БыстрыйДоступ,
			"ОтборПользователей");
	Иначе
		ЭлементОтбораПользователь.ВидСравнения = ВидСравненияКомпоновкиДанных.ВСписке;
		ЭлементОтбораПользователь.РежимОтображения = РежимОтображенияЭлементаНастройкиКомпоновкиДанных.БыстрыйДоступ;
		Если Не ЗначениеЗаполнено(ЭлементОтбораПользователь.ИдентификаторПользовательскойНастройки) Тогда
			ЭлементОтбораПользователь.ИдентификаторПользовательскойНастройки = "ОтборПользователей";
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

// Устанавливает список значений для стандартного отбора по полю Пользователь.
// Значение элемента списка совпадает со строкой, записанной в регистр замеров.
Процедура ПриОпределенииПараметровВыбора(Форма, СвойстваНастройки) Экспорт
	
	Если СвойстваНастройки.ПолеКД <> Новый ПолеКомпоновкиДанных("Пользователь") Тогда
		Возврат;
	КонецЕсли;
	
	ИсторическиеЗначения = ПолучитьИсторическиеПредставленияПользователей(Форма);
	РезультатПолучения = ПолучитьТекущиеПредставленияПользователей(ИсторическиеЗначения);
	
	СвойстваНастройки.ЗапросЗначенийВыбора.Текст = "";
	Если Не РезультатПолучения.Доступен Тогда
		СвойстваНастройки.ОграничиватьВыборУказаннымиЗначениями = Истина;
		СообщитьОНедоступностиСпискаПользователей();
		Возврат;
	КонецЕсли;
	
	ДобавленныеЗначения = Новый Соответствие;
	Для Каждого ЭлементСписка Из СвойстваНастройки.ЗначенияДляВыбора Цикл
		ДобавленныеЗначения.Вставить(Строка(ЭлементСписка.Значение), Истина);
	КонецЦикла;
	
	Для Каждого ЭлементСписка Из РезультатПолучения.Значения Цикл
		ДобавитьЗначениеВыбора(
			СвойстваНастройки.ЗначенияДляВыбора,
			ДобавленныеЗначения,
			ЭлементСписка.Значение,
			ЭлементСписка.Представление);
	КонецЦикла;
	
	Для Каждого ЭлементСписка Из ИсторическиеЗначения Цикл
		Если ДобавленныеЗначения.Получить(Строка(ЭлементСписка.Значение)) = Неопределено Тогда
			ДобавитьЗначениеВыбора(
				СвойстваНастройки.ЗначенияДляВыбора,
				ДобавленныеЗначения,
				ЭлементСписка.Значение,
				НСтр("ru = '[Исторический] '") + ЭлементСписка.Представление);
		КонецЕсли;
	КонецЦикла;
	
	СвойстваНастройки.ОграничиватьВыборУказаннымиЗначениями = Истина;
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеФункцииОтбораПользователей

Процедура СообщитьОНедоступностиСпискаПользователей()
	
	Если ПредупреждениеОНедоступностиСпискаПользователейПоказано Тогда
		Возврат;
	КонецЕсли;
	
	ПредупреждениеОНедоступностиСпискаПользователейПоказано = Истина;
	Сообщить(
		НСтр("ru = 'Список пользователей недоступен. Отбор по пользователям отключён для текущего сеанса'"),
		СтатусСообщения.Внимание);
	
КонецПроцедуры

Процедура ДобавитьЗначениеВыбора(ЗначенияДляВыбора, ДобавленныеЗначения, Значение, Представление)
	
	КлючЗначения = Строка(Значение);
	Если ДобавленныеЗначения.Получить(КлючЗначения) <> Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ЗначенияДляВыбора.Добавить(Значение, Представление);
	ДобавленныеЗначения.Вставить(КлючЗначения, Истина);
	
КонецПроцедуры

// Возвращает текущих пользователей информационной базы в формате:
// <Имя пользователя ИБ>; <Наименование элемента справочника Пользователи>.
Функция ПолучитьТекущиеПредставленияПользователей(ИсторическиеЗначения)
	
	Результат = Новый Структура;
	Результат.Вставить("Доступен", Истина);
	Результат.Вставить("Значения", Новый СписокЗначений);
	Результат.Вставить("ТекстОшибки", "");
	
	Попытка
		ОписанияПользователейИБ = ПользователиИнформационнойБазы.ПолучитьПользователей();
	Исключение
		Результат.Доступен = Ложь;
		Результат.ТекстОшибки = ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		Возврат Результат;
	КонецПопытки;
	
	ПользователиПриложения = ПолучитьПользователейПриложения(ОписанияПользователейИБ);
	ДобавленныеПредставления = Новый Соответствие;
	
	Для Каждого ОписаниеПользователяИБ Из ОписанияПользователейИБ Цикл
		
		ИмяПользователяИБ = СокрЛП(ОписаниеПользователяИБ.Имя);
		Если ПустаяСтрока(ИмяПользователяИБ) Тогда
			Продолжить;
		КонецЕсли;
		
		ПользовательПриложения = ПользователиПриложения.Получить(
			Строка(ОписаниеПользователяИБ.УникальныйИдентификатор));
		
		Если ПользовательПриложения <> Неопределено Тогда
			ПредставлениеПользователя = ИмяПользователяИБ + "; " + ПользовательПриложения.Наименование;
			ДобавитьТекущееПредставлениеПользователя(
				Результат.Значения,
				ДобавленныеПредставления,
				ПредставлениеПользователя);
		Иначе
			ДобавленоИсторическоеПредставление = ДобавитьИсходныеПредставленияПользователя(
				Результат.Значения,
				ДобавленныеПредставления,
				ИсторическиеЗначения,
				ИмяПользователяИБ);
			
			Если Не ДобавленоИсторическоеПредставление Тогда
				ДобавитьТекущееПредставлениеПользователя(
					Результат.Значения,
					ДобавленныеПредставления,
					ИмяПользователяИБ);
			КонецЕсли;
		КонецЕсли;
		
	КонецЦикла;
	
	Результат.Значения.СортироватьПоПредставлению();
	Возврат Результат;
	
КонецФункции

Функция ПолучитьПользователейПриложения(ОписанияПользователейИБ)
	
	Результат = Новый Соответствие;
	Идентификаторы = Новый Массив;
	
	Для Каждого ОписаниеПользователяИБ Из ОписанияПользователейИБ Цикл
		Идентификаторы.Добавить(ОписаниеПользователяИБ.УникальныйИдентификатор);
	КонецЦикла;
	
	Если Идентификаторы.Количество() = 0 Тогда
		Возврат Результат;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Пользователи.ИдентификаторПользователяИБ КАК ИдентификаторПользователяИБ,
	|	Пользователи.Наименование КАК Наименование
	|ИЗ
	|	Справочник.Пользователи КАК Пользователи
	|ГДЕ
	|	Пользователи.ИдентификаторПользователяИБ В (&Идентификаторы)";
	Запрос.УстановитьПараметр("Идентификаторы", Идентификаторы);
	
	Попытка
		Выборка = Запрос.Выполнить().Выбрать();
		Пока Выборка.Следующий() Цикл
			ОписаниеПользователя = Новый Структура("Наименование", Выборка.Наименование);
			Результат.Вставить(
				Строка(Выборка.ИдентификаторПользователяИБ),
				ОписаниеПользователя);
		КонецЦикла;
	Исключение
		// При отсутствии прав составное представление дополняется историческим значением,
		// а при его отсутствии используется имя пользователя информационной базы.
	КонецПопытки;
	
	Возврат Результат;
	
КонецФункции

Процедура ДобавитьТекущееПредставлениеПользователя(Значения, ДобавленныеПредставления, ПредставлениеПользователя)
	
	Если ДобавленныеПредставления.Получить(ПредставлениеПользователя) <> Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Значения.Добавить(ПредставлениеПользователя, ПредставлениеПользователя);
	ДобавленныеПредставления.Вставить(ПредставлениеПользователя, Истина);
	
КонецПроцедуры

Функция ДобавитьИсходныеПредставленияПользователя(
	Значения,
	ДобавленныеПредставления,
	ИсторическиеЗначения,
	ИмяПользователяИБ)
	
	Добавлено = Ложь;
	ПрефиксПользователя = ИмяПользователяИБ + ";";
	Для Каждого ИсторическоеЗначение Из ИсторическиеЗначения Цикл
		ПредставлениеПользователя = Строка(ИсторическоеЗначение.Значение);
		Если ПредставлениеПользователя = ИмяПользователяИБ
			Или СтрНачинаетсяС(ПредставлениеПользователя, ПрефиксПользователя) Тогда
			ДобавитьТекущееПредставлениеПользователя(
				Значения,
				ДобавленныеПредставления,
				ПредставлениеПользователя);
			Добавлено = Истина;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Добавлено;
	
КонецФункции

// Возвращает представления пользователей, реально записанные в регистре замеров
// за выбранный в отчете период.
Функция ПолучитьИсторическиеПредставленияПользователей(Форма)
	
	Значения = Новый СписокЗначений;
	ГраницыПериода = ПолучитьГраницыПериодаОтчета(Форма);
	Если Не ГраницыПериода.Определены Тогда
		Возврат Значения;
	КонецЕсли;
	
	ИмяРегистраЗамеров = ИмяРегистраЗамеровВремени();
	Запрос = Новый Запрос;
	Запрос.Текст = СтрШаблон(
		"ВЫБРАТЬ РАЗЛИЧНЫЕ
		|    Замеры.Пользователь КАК Пользователь
		|ИЗ
		|    %1 КАК Замеры
		|ГДЕ
		|    Замеры.ДатаЗаписи МЕЖДУ &НачалоПериода И &КонецПериода
		|    И Замеры.Пользователь <> """"
		|УПОРЯДОЧИТЬ ПО
		|    Пользователь",
		ИмяРегистраЗамеров);
	Запрос.УстановитьПараметр("НачалоПериода", ГраницыПериода.Начало);
	Запрос.УстановитьПараметр("КонецПериода", ГраницыПериода.Конец);
	
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		ПредставлениеПользователя = Строка(Выборка.Пользователь);
		Значения.Добавить(ПредставлениеПользователя, ПредставлениеПользователя);
	КонецЦикла;
	
	Возврат Значения;
	
КонецФункции

Функция ПолучитьГраницыПериодаОтчета(Форма)
	
	Результат = Новый Структура("Определены, Начало, Конец", Ложь, Неопределено, Неопределено);
	ПараметрПериод = Форма.Отчет.КомпоновщикНастроек.Настройки.ПараметрыДанных.Элементы.Найти("Период");
	Если ПараметрПериод = Неопределено Или Не ПараметрПериод.Использование Тогда
		Возврат Результат;
	КонецЕсли;
	
	Попытка
		Результат.Начало = ПараметрПериод.Значение.ДатаНачала;
		Результат.Конец = ПараметрПериод.Значение.ДатаОкончания;
	Исключение
		Возврат Результат;
	КонецПопытки;
	
	Результат.Определены = ЗначениеЗаполнено(Результат.Начало)
		И ЗначениеЗаполнено(Результат.Конец);
	Возврат Результат;
	
КонецФункции

Функция ИмяРегистраЗамеровВремени()
	
	ИмяРегистра = "РегистрСведений.ЗамерыВремени";
	ИмяРегистраТехнологическихЗамеров = "РегистрСведений.ЗамерыВремениТехнологические";
	Если Метаданные.НайтиПоПолномуИмени(ИмяРегистраТехнологическихЗамеров) <> Неопределено Тогда
		ИмяРегистра = ИмяРегистраТехнологическихЗамеров;
	КонецЕсли;
	
	Возврат ИмяРегистра;
	
КонецФункции

#КонецОбласти

#Область ОбработчикиСобытий

Процедура ПриКомпоновкеРезультата(ДокументРезультат, ДанныеРасшифровки, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	СхемаКомпоновкиДанных = ПолучитьМакет("ОсновнаяСхемаКомпоновкиДанных");
	
	ИмяРегистраТехнологическихЗамеров = "РегистрСведений.ЗамерыВремениТехнологические";
	Если НЕ Метаданные.НайтиПоПолномуИмени(ИмяРегистраТехнологическихЗамеров) = Неопределено Тогда		
		ЗапросИсточника = СхемаКомпоновкиДанных.НаборыДанных.ЗамерыПроизводительности.Запрос;	
		СхемаЗапросаИсточника = Новый СхемаЗапроса;
		СхемаЗапросаИсточника.УстановитьТекстЗапроса(ЗапросИсточника);
		ОператорыЗапросаИсточника = СхемаЗапросаИсточника.ПакетЗапросов.Получить(0).Операторы;
		ИсточникТехнологическихЗамеров = ОператорыЗапросаИсточника.Добавить(ОператорыЗапросаИсточника.Получить(0));
		ИсточникТехнологическихЗамеров.ВыбираемыеПоля.Установить(17, Новый ВыражениеСхемыЗапроса("ИСТИНА"));
		ДоступныйИсточникТехнологическихЗамеров = СхемаЗапросаИсточника.ПакетЗапросов[0].ДоступныеТаблицы[7].Состав.Найти(ИмяРегистраТехнологическихЗамеров);
		ИсточникТехнологическихЗамеров.Источники.Заменить(0, ДоступныйИсточникТехнологическихЗамеров);
		СхемаКомпоновкиДанных.НаборыДанных.ЗамерыПроизводительности.Запрос = СхемаЗапросаИсточника.ПолучитьТекстЗапроса();
	КонецЕсли;
	
	Настройки = КомпоновщикНастроек.ПолучитьНастройки();         
	ДанныеРасшифровки = Новый ДанныеРасшифровкиКомпоновкиДанных;          
	КомпоновщикМакета = Новый КомпоновщикМакетаКомпоновкиДанных;          
	МакетКомпоновки = КомпоновщикМакета.Выполнить(СхемаКомпоновкиДанных, Настройки, ДанныеРасшифровки);
	
	ПроцессорКомпоновкиДанных = Новый ПроцессорКомпоновкиДанных;
	ПроцессорКомпоновкиДанных.Инициализировать(МакетКомпоновки,,ДанныеРасшифровки);
	
	ПроцессорВывода = Новый ПроцессорВыводаРезультатаКомпоновкиДанныхВТабличныйДокумент;
	ПроцессорВывода.УстановитьДокумент(ДокументРезультат);
	
	ПроцессорВывода.НачатьВывод();
	ЭлементРезультатКомпоновки = ПроцессорКомпоновкиДанных.Следующий();
	Пока ЭлементРезультатКомпоновки <> Неопределено Цикл
		ПроцессорВывода.ВывестиЭлемент(ЭлементРезультатКомпоновки);
		ЭлементРезультатКомпоновки = ПроцессорКомпоновкиДанных.Следующий();
	КонецЦикла;
	ПроцессорВывода.ЗакончитьВывод();	
	
КонецПроцедуры

#КонецОбласти